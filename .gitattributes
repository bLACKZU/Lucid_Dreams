# Shell scripts must keep LF endings or they break on Linux/macOS
# ("bad interpreter: /usr/bin/env bash^M") when committed from Windows.
*.sh text eol=lf

# PowerShell scripts are Windows-side; CRLF is fine and expected there.
*.ps1 text eol=crlf
